grafana chart installed.

Smoke test:

1. kubectl -n {{ .Release.Namespace }} get pods

2. Open http://grafana.sheepdog.io
   (requires minikube tunnel + hosts-file entry: 127.0.0.1 grafana.sheepdog.io)

3. Log in as admin (password below):

   kubectl -n {{ .Release.Namespace }} get secret grafana \
     -o jsonpath='{.data.admin-password}' | base64 -d

Upcoming steps (sheep-dog-main#252):
- 3b: datasource + dashboard provisioning as code
- 3c: CSV transport for Darmok metrics.csv
- 3d: Infinity datasource plugin
- 3e: KensoBI SPC panel plugin + XmR dashboard
